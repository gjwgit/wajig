Upgrade all of the installed packages.
$ wajig upgrade
options:
  -b            backup packages about to be upgrade onto some default directory
  --backup=DIR  backup packages about to be upgrade onto a given directory
  -n --noauth   skip the authentication verification before the upgrade
