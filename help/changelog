Display Debian changelog for a given package.
$ wajig changelog PKG
options:
  -v --verbose=1    if there's newer entries, display them, and proceed to
                    display complete local changelog
  -P --pager        same as -v, but use a pager (EG, less)
