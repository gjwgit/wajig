# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/wajig.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall wajig
	if grep -qs '^wajig$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^wajig$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
