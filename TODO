gradual
* use suprocess module instead of older os.system() and os.popen()

future
* 'wajig install DEBFILE' should only autocomplete filenames
* 'wajig reconfigure SCRIPTNAME' should only autocomplete /etc/init.d/*
* add auto completion for wajig shell
